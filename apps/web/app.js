const app=document.querySelector('#app');const api='/api';let token=localStorage.token||'',theme=localStorage.theme||'light';document.body.className=theme==='dark'?'dark':'';
const nav=()=>`<nav class="nav wrap"><a class="brand" href="/" onclick="go('home');return false"><i></i>SANADCHAIN</a><div class="navlinks"><a href="#" onclick="go('verify')">Verify</a><a href="#" onclick="go('security')">Security demo</a><a href="#" onclick="go('onboarding')">Onboarding</a><a href="#" onclick="go('dashboard')">Console</a><button onclick="toggleTheme()">◐</button><button class="alt" onclick="go('login')">Institution login</button></div></nav>`;const foot=()=>`<footer class="footer wrap">© 2026 SanadChain · Privacy-first academic trust infrastructure · <span class="mono">DEMO MODE</span></footer>`;const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function go(page){history.pushState({},'',page==='home'?'/':'/'+page);render()}function toggleTheme(){theme=theme==='dark'?'light':'dark';localStorage.theme=theme;document.body.className=theme==='dark'?'dark':''}window.go=go;window.toggleTheme=toggleTheme;
const home=()=>`${nav()}<main><section class="hero"><div class="wrap"><div class="eyebrow">Academic credential infrastructure</div><h1>Trust every credential.<br>Verify in seconds.</h1><p>Blockchain-anchored academic credentials for fast, secure and tamper-evident verification.</p><div class="actions"><a class="button" href="#" onclick="go('verify')">Verify a credential →</a><a class="button alt" href="#" onclick="go('login')">Institution login</a></div><div class="flow"><span>ISSUE</span><b>→</b><span>HASH</span><b>→</b><span>SIGN</span><b>→</b><span>LEDGER</span><b>→</b><span>QR</span><b>→</b><span>VERIFY</span></div></div></section><section class="section wrap"><div class="eyebrow">Purpose-built trust</div><h2>Proof, not paperwork.</h2><p class="lead">SanadChain preserves the evidence that matters: a document hash, issuer signature, lifecycle status and immutable-style audit trail. Full documents remain off-chain.</p><div class="grid"><article class="card"><div class="icon">✓</div><h3>Instant verification</h3><p>Employers verify from a QR or credential ID — no account and no email chain required.</p></article><article class="card"><div class="icon">#</div><h3>Tamper-evident by design</h3><p>SHA-256 proofs make an altered certificate immediately detectable.</p></article><article class="card"><div class="icon">◇</div><h3>Institution controlled</h3><p>Role-based issuance and revocation, designed for Hyperledger Fabric identities.</p></article></div></section><section class="band"><div class="wrap"><div class="eyebrow" style="color:#72dcf0">DEMO MODE</div><h2>Development Blockchain Simulator</h2><p class="lead">This live demo uses a simulator with the same service interface as the Fabric adapter. It never claims to be a production Fabric network.</p><div class="actions"><a class="button" href="#" onclick="go('security')">Run tamper demo</a><a class="button alt" style="color:#fff;border-color:#ffffff55" href="#" onclick="go('onboarding')">Institution onboarding</a></div></div></section></main>${foot()}`;
const verify=()=>`${nav()}<main class="verify"><div class="eyebrow">Public verification portal</div><h1>Verify a credential</h1><p class="lead">Enter the credential ID from the document or QR code. Only minimum necessary information is displayed.</p><div class="card"><div class="verifybox"><input id="credentialId" value="SANAD-2026-000123" aria-label="Credential ID"><button onclick="verifyCredential()">Verify now</button></div><small>Try <b>SANAD-2026-000123</b> (valid) or <b>SANAD-2026-000124</b> (revoked).</small><div id="result"></div></div></main>${foot()}`;
async function verifyCredential(){const id=document.querySelector('#credentialId').value.trim(),box=document.querySelector('#result');box.innerHTML='<p class="lead">Checking ledger proof…</p>';const r=await fetch(`${api}/verify/${encodeURIComponent(id)}`),d=await r.json();if(!r.ok){box.innerHTML=`<div class="result card"><div class="status revoked">✕ ${esc(d.status||'NOT FOUND')}</div><p>${esc(d.message)}</p></div>`;return}const c=d.credential,cls=d.status==='VALID'?'valid':'revoked';box.innerHTML=`<div class="result card"><div class="status ${cls}">${d.status==='VALID'?'✓ CREDENTIAL VERIFIED':'⚠ CREDENTIAL REVOKED'}</div><p>${d.status==='VALID'?'Hash matched, issuer identity verified and ledger confirmation found.':'This credential has been revoked by the issuing institution.'}</p><div class="details"><div class="detail"><label>Institution</label>${esc(c.institution)}</div><div class="detail"><label>Credential</label>${esc(c.program)}</div><div class="detail"><label>Issue date</label>${esc(c.issueDate)}</div><div class="detail"><label>Verification time</label>${d.verificationSeconds}s (actual API measurement)</div><div class="detail"><label>Credential ID</label><span class="mono">${esc(c.credentialId)}</span></div><div class="detail"><label>Blockchain status</label>CONFIRMED · Block #${c.blockNumber}</div><div class="detail"><label>Issuer</label>${esc(c.issuer)}</div><div class="detail"><label>Transaction</label><span class="mono">${esc(c.transactionId)}</span></div></div>${c.revocationReason?`<p class="notice">Reason: ${esc(c.revocationReason)}</p>`:''}</div>`}window.verifyCredential=verifyCredential;
const security=()=>`${nav()}<main class="section wrap"><div class="eyebrow">Security demonstration</div><h1>Tamper detection, demonstrated.</h1><p class="lead">Upload a file to compare its SHA-256 fingerprint with the demo ledger credential. Select the original demo text file below for a valid result; any modified file becomes tampered.</p><div class="form"><div class="card"><label>Credential to check<select id="hashCred"><option>SANAD-2026-000123</option><option>SANAD-2026-000124</option></select></label><label>Document file<input id="hashFile" type="file" aria-label="Credential document"></label><button onclick="checkHash()">Generate hash & verify</button><div id="hashResult"></div></div></div><div class="grid"><div class="card"><div class="icon">✓</div><h3>Genuine</h3><p>Hash matches the cryptographic proof and credential is active.</p></div><div class="card"><div class="icon">×</div><h3>Tampered</h3><p>Any altered bytes result in a different SHA-256 hash.</p></div><div class="card"><div class="icon">!</div><h3>Revoked</h3><p>The original proof remains, while a new lifecycle event blocks trust.</p></div></div></main>${foot()}`;
async function checkHash(){const f=document.querySelector('#hashFile').files[0],id=document.querySelector('#hashCred').value,box=document.querySelector('#hashResult');if(!f){box.innerHTML='<p class="notice">Choose a document first. For demo data, use a text file containing demo-document-SANAD-2026-000123.</p>';return}const data=await f.text(),r=await fetch(`${api}/verify/hash?credentialId=${id}`,{method:'POST',headers:{'Content-Type':'text/plain'},body:data}),d=await r.json(),cls=d.status==='VALID'?'valid':d.status==='TAMPERED'?'tampered':'revoked';box.innerHTML=`<div class="result"><div class="status ${cls}">${d.status==='VALID'?'✓ GENUINE':d.status==='TAMPERED'?'✕ TAMPER DETECTED':'⚠ CREDENTIAL REVOKED'}</div><p>${esc(d.message)}</p><p class="mono">Original: ${d.originalHash}<br>Uploaded: ${d.uploadedHash}</p></div>`}window.checkHash=checkHash;
const onboarding=()=>`${nav()}<main class="section wrap"><div class="eyebrow">Institution onboarding</div><h1>From registration to issuing.</h1><p class="lead">A guided workflow designed to reduce institutional friction while preserving network governance.</p><div class="onboard"><div class="steps"><div><b>01 · Registration</b>Institution profile and accreditation evidence</div><div><b>02 · Verification</b>Authority review and approval status</div><div><b>03 · Blockchain identity</b>Fabric organization identity provisioned</div><div><b>04 · Issuer setup</b>Authorized officers and role assignments</div><div><b>05 · Ready to issue</b>Credential lifecycle becomes available</div></div><form class="card" onsubmit="event.preventDefault();this.innerHTML='<h2>Application received</h2><p>ABC University-style onboarding is now <b>PENDING REVIEW</b>. In Fabric mode, identity provisioning begins only after authority approval.</p>'"><h3>Start an application</h3><label>Institution name<input required placeholder="Example University"></label><label>Official email<input required type="email" placeholder="registrar@example.edu"></label><label>Institution type<select><option>University</option><option>College</option><option>Technical institute</option></select></label><label>Accreditation reference<input placeholder="Accreditation / university ID"></label><button>Submit for review</button></form></div></main>${foot()}`;
const loginPage=()=>`${nav()}<main class="section form"><div class="eyebrow">Secure institution access</div><h1>Welcome back</h1><div class="notice">Demo account: admin@abc.edu · Demo@123</div><form class="card" onsubmit="login(event)"><label>Email<input id="email" type="email" value="admin@abc.edu" required></label><label>Password<input id="password" type="password" value="Demo@123" required></label><button>Sign in</button><div id="loginError"></div></form></main>${foot()}`;
async function login(e){e.preventDefault();const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email.value,password:password.value})}),d=await r.json();if(!r.ok){loginError.textContent=d.error;return}token=d.token;localStorage.token=token;go('dashboard')}window.login=login;
async function dashboard(){app.innerHTML=`${nav()}<main class="dashboard wrap"><div class="eyebrow">Institution console · ABC University</div><h1>Credential operations</h1><div class="stats"><div class="card stat"><strong>248</strong><small>Credentials issued</small></div><div class="card stat"><strong>96.4%</strong><small>Verification success rate</small></div><div class="card stat"><strong>12</strong><small>Verified this week</small></div><div class="card stat"><strong>3</strong><small>Revoked records</small></div></div><div class="actions" style="margin:25px 0"><button onclick="go('issue')">+ Issue credential</button><button class="alt" onclick="network()">Network status</button></div><section class="card"><h3>Credential register</h3><div id="credentialTable">Loading records…</div></section><section class="card" style="margin-top:18px"><h3>Audit trail</h3><div id="audit">Loading activity…</div></section></main>${foot()}`;if(!token){go('login');return}const headers={Authorization:`Bearer ${token}`};const [cr,ar]=await Promise.all([fetch('/api/credentials',{headers}),fetch('/api/audit',{headers})]);if(cr.status===401){token='';localStorage.removeItem('token');go('login');return}const cs=(await cr.json()).items,as=(await ar.json()).items;credentialTable.innerHTML=`<table><thead><tr><th>ID</th><th>Program</th><th>Status</th><th>Action</th></tr></thead><tbody>${cs.map(c=>`<tr><td class="mono">${c.credentialId}</td><td>${esc(c.program)}</td><td><span class="tag ${c.status==='REVOKED'?'red':''}">${c.status}</span></td><td>${c.status==='ACTIVE'?`<button class="alt" onclick="revoke('${c.credentialId}')">Revoke</button>`:'—'}</td></tr>`).join('')}</tbody></table>`;audit.innerHTML=as.slice(0,5).map(a=>`<p><b>${esc(a.action)}</b> · ${esc(a.resource)} <small>${new Date(a.timestamp).toLocaleString()}</small></p>`).join('')}
async function revoke(id){const reason=prompt('Public-safe revocation reason:','Administrative error');if(!reason)return;await fetch(`/api/credentials/${id}/revoke`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({reason})});dashboard()}window.revoke=revoke;async function network(){const d=await (await fetch('/api/blockchain/status')).json();alert(`${d.adapter}\n${d.peers} peers · ${d.organizations} organizations\nOrdering service: ${d.orderingService}\nChaincode: ${d.chaincode}\nLatest block: #${d.latestBlock}`)}window.network=network;
const issue=()=>`${nav()}<main class="section form"><div class="eyebrow">Credential issuance</div><h1>Create a credential</h1><p class="lead">The source document is represented by a SHA-256 proof; it is never written to the ledger.</p><form class="card" onsubmit="issueCredential(event)"><label>Credential type<select id="credentialType"><option>Degree</option><option>Diploma</option><option>Marksheet</option><option>Transcript</option><option>Certificate</option></select></label><label>Student display name<input id="studentDisplayName" required placeholder="e.g. Priya K."></label><label>Student reference ID<input id="studentReference" required placeholder="STU-2026-042"></label><label>Program<input id="program" required placeholder="B.Tech Computer Science"></label><label>Document content / reference<input id="documentContent" required placeholder="A protected document reference"></label><button>Hash, sign & submit to ledger</button><div id="issued"></div></form></main>${foot()}`;
async function issueCredential(e){e.preventDefault();const body={credentialType:credentialType.value,studentDisplayName:studentDisplayName.value,studentReference:studentReference.value,program:program.value,documentContent:documentContent.value};const r=await fetch('/api/credentials',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify(body)}),d=await r.json();if(!r.ok){issued.innerHTML=`<p class="notice">${esc(d.error)}</p>`;return}issued.innerHTML=`<div class="result"><div class="status valid">✓ BLOCKCHAIN CONFIRMED</div><p>Credential <b>${d.credentialId}</b> was hashed, digitally signed and anchored in block #${d.blockNumber}.</p><img src="${d.qr}" width="130" alt="QR code for credential verification"><p class="mono">${d.verificationUrl}</p></div>`}window.issueCredential=issueCredential;
function render(){const path=location.pathname.split('/')[1]||'home';if(path==='verify'&&location.pathname.split('/')[2]){app.innerHTML=verify();credentialId.value=location.pathname.split('/')[2];verifyCredential();return}if(path==='dashboard'){dashboard();return}app.innerHTML=({home,verify,security,onboarding,login:loginPage,issue}[path]||home)()}window.onpopstate=render;render();
